<!-- Gemini CLI adapter: GEMINI.md is the file Gemini CLI auto-loads; the
     canonical instructions live in AGENTS.md (harness-neutral). Install this
     file at the repo root. (Recent Gemini CLI versions can be configured to
     read AGENTS.md directly via settings contextFileName — prefer that and
     drop this file if your version supports it.) -->

Read and follow `AGENTS.md` in this repository root — it is the canonical
instructions file for all agents working here. Then run
`bash tools/bootstrap.sh` before other work.

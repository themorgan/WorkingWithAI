# Review-only slide (internal)

This slide is marked `review_only` in the manifest. It appears in the
review build and is **absent from the send build** — the place for
internal caveats, open questions, and staging notes that must never
travel with an external copy.

<!-- notes -->
If you can read this in a file you received from someone, the wrong build
was sent.

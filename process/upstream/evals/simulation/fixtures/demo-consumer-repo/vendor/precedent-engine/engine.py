# Fixture stub -- see ../../README.md. Stands in for a vendored copy of
# Precedent's own engine scripts, the way a real dependent repo would keep
# one under its own vendor/ path rather than BestPractice's process/upstream/.

def run_checks():
    raise NotImplementedError('fixture stub')

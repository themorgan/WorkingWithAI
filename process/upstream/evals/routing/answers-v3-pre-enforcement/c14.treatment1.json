{"slugs": ["convention-to-audit", "cite-the-incident", "computed-numbers-in-scripts", "doc-references-are-links"], "reasoning": "Both audits promote a costly, silently-broken convention into a script that exits non-zero (convention-to-audit), and the new PRACTICES.md rule records the failures that motivated it -- orphan blocks with a live placeholder, and the two false-positive traps -- inline with the rule (cite-the-incident). The doc_sync PAIRS registry and sentinel/emitter contract being verified is exactly the generated-block mechanism of computed-numbers-in-scripts, and the PRACTICES.md edit is a document edit that must use relative links and the correct approximately sign (the added prose writes ~91% rather than the required form)."}

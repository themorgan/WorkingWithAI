{"slugs": ["verify-postcondition", "docs-are-current-state", "cite-the-incident", "scrub-gate", "reply-links-files"], "reasoning": "Reinstating erased blocks is a state-changing document edit, so the postcondition to test is that the three blocks are actually present and match their source commits verbatim, and the restored text must read as current state (no \"restored on DATE\" annotation) — which is also exactly what the reinstated P28 placement rule itself resolves. The blocks carry their origin incidents, which must travel with them intact; the text originates in a dependent repo's history scan and lands in the public upstream tree, so it gets the second scrub pass; and the reply ends with a Files touched entry for PRACTICES.md."}

{"slugs": ["mistakes-become-rules", "cite-the-incident"], "reasoning": "The change fixes a defect found after a new gate shipped and explicitly states a general lesson ('a new gate is not done until the whole loop has been walked with it in place'), so the miss must be root-caused and encoded as a rule rather than left in a commit message; and because that rule is being written, the failure it prevents belongs inline with it, which is what the new code comment does. Nothing here involves merging, generated artifacts, outward-facing documents, or computed figures, so no other index entry applies."}

{"slugs": ["mistakes-become-rules", "cite-the-incident", "convention-to-audit", "practice-export-loop", "merge-runbook"], "reasoning": "A caught content-loss mistake is root-caused and prevented at the strongest rung, a mechanical gate in checkin.py's record() (mistakes-become-rules, convention-to-audit), with the origin incident written inline in both the new INSTALL.md rule and the script's own docstring (cite-the-incident); the loss happened because one thread's sync silently overwrote another thread's concurrent addition to the same shared vendored tree instead of carrying both forward, which is exactly what merge-runbook's per-file-class resolution rules exist to prevent, and the whole mechanism being hardened is checkin.py/manifest.json, the machinery of practice-export-loop."}
{"slugs": ["session-bootstrap", "verify-postcondition"], "reasoning": "The commit is explicitly an extension of session-bootstrap to the other end of a turn (a stop hook mirroring the session-start hook, both hard-guaranteed via the harness rather than agent memory), and the hook itself is a direct implementation of verify-postcondition -- it mechanically checks the exact named postconditions ('no unpushed commits on any branch', no uncommitted/untracked files) rather than trusting the agent remembered to do so."}
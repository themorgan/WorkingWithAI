{"slugs": ["convention-to-audit", "cite-the-incident", "mistakes-become-rules"], "reasoning": "Both additions promote a costly hand-drifting convention (an unwired sentinel, an unsourced quantity) into a script that fails the build, exactly convention-to-audit's move; the commit message and code comments record the specific failures that motivated each rule (orphan blocks, ~91% unexplained quantities, the marker self-triggering, the unit-boundary false positive), which is cite-the-incident; and the two false-positive traps found on first run were root-caused and encoded as prevention (unit-boundary matching, marker-alone-on-line), which is mistakes-become-rules."}

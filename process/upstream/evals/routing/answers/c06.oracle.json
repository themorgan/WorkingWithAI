{"slugs": ["verify-postcondition", "practice-export-loop"], "reasoning": "The bug and its own commit message are almost a textbook case of verify-postcondition (\"a new gate is not done when it passes its own test, it is done when the whole loop has been walked with it installed\"), and the fix is to tools/checkin.py's manifest-tracking fields, the core mechanism of the practice-export-loop that keeps a dependent repo's vendored tree and upstream in sync."}
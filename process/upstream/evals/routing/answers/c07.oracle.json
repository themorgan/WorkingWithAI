{"slugs": ["layered-practice-packs", "practice-export-loop", "variant-re-derives", "registry-source-of-truth"], "reasoning": "The change deliberately classifies VOICE.md/STYLEGUIDE.md as repo-local identity rather than generic practice (layered-practice-packs), wires that decision into the export gate so they never answer yes to the check-in question (practice-export-loop), and records their status in the manifest, the one source of truth for synced/local-only state (registry-source-of-truth); crucially the authors did NOT blindly reuse the existing 'instantiate and adapt like other templates' pattern for these two new templates, explicitly re-deriving that constraint against the new files' different nature (variant-re-derives)."}
{"slugs": ["mistakes-become-rules", "cite-the-incident", "verify-postcondition"], "reasoning": "The commit root-causes a bug found by walking the whole cycle (not just testing the guard in isolation) and records the failure story inline in the code comment, matching mistakes-become-rules and cite-the-incident; the underlying miss was checking that the guard passed its own test rather than verifying the actual postcondition (currency) end-to-end, which is exactly verify-postcondition."}

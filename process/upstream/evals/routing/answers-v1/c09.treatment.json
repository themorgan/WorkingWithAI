{"slugs": ["docs-track-models", "convention-to-audit", "scripts-assert-properties", "cite-the-incident"], "reasoning": "This enforces practice 33 (docs-track-models) rule 2 itself, so its full text is needed to implement the restatement check correctly; the work turns a stated-but-unenforced convention into a script that exits non-zero (convention-to-audit); it adds owned_figures() so scripts declare/assert the figures they own for a doc to cite (scripts-assert-properties); and the PRACTICES.md addition documents the new rule with the incident that motivated it (cite-the-incident)."}

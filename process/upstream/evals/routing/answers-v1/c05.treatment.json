{"slugs": ["engine-plus-host-shims", "session-bootstrap", "cite-the-incident", "doc-references-are-links"], "reasoning": "checkin.py is the vendored engine exported across the repo boundary and templates/bootstrap.sh is its thin host shim, wiring the new fresh subcommand into the session-start hook so detection lives there instead of in memory; the PRACTICES.md addition writes a new convention (automated detection, deliberate take) that should cite the incident behind it, and it references INSTALL.md by bare name rather than as a relative link."}

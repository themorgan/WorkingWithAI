{"slugs": ["cite-the-incident", "convention-to-audit", "mistakes-become-rules", "scripts-assert-properties"], "reasoning": "This diff is the authoring of practice 30 itself (scripts-assert-properties) plus tools/model_audit.py, which promotes a prose convention into a failing audit script (convention-to-audit); the new rule and script docstring carry the origin incident inline (cite-the-incident); and the practice's own text explicitly says it was produced by the five-whys mistakes-become-rules process, including a retracted first root-cause. No auto-surfaced document-formatting practice is actually implicated -- the file/function mentions are code-style backticks consistent with existing usage, not prose that skips a required link, and no computed figures or dated annotations appear."}
{"slugs": ["mistakes-become-rules", "cite-the-incident", "practice-export-loop"], "reasoning": "This is a dependent repo's check-in that root-caused a caught mistake (an agent stamping a volatile-rule date from its own clock instead of the contributor's local date) into a new corollary on practice 16, which is exactly mistakes-become-rules' encode-the-prevention step and the occasion index's own mapping for both 'a mistake is caught' and 'writing a new convention or rule' (the new corollary explains the failure mode inline, satisfying cite-the-incident); landing as a PR into BestPractice's own AGENTS.md/PRACTICES.md/template is the practice-export-loop's periodic propose-back step."}

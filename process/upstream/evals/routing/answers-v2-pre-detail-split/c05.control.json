{"slugs": ["engine-plus-host-shims", "session-bootstrap", "capture-gate", "cite-the-incident"], "reasoning": "The fresh() check is added to the vendored checkin.py engine and wired in via the templates/bootstrap.sh host shim (engine-plus-host-shims), it extends the session-start hook so staleness detection is automatic rather than relied-on-memory (session-bootstrap, the very practice section this text was inserted into), the doc updates to INSTALL.md and PRACTICES.md land in the same commit as the code before merge (capture-gate), and the new text states inline why the take must stay deliberate -- unattended mirrors are the mechanism class that loses content (cite-the-incident). It deliberately stays a notice rather than a gate, so convention-to-audit does not apply."}

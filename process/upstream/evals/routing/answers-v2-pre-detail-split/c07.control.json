{"slugs": ["variant-re-derives", "registry-source-of-truth", "practice-export-loop"], "reasoning": "VOICE.md/STYLEGUIDE.md are a new variant of the existing install-time template pattern (MAP.md.template, GLOSSARY.md.template, etc.), so each inherited attribute -- whether the file is silently rewritten from subject matter, and whether it can ever be exported -- is deliberately re-derived rather than assumed (variant-re-derives); the local-only/never-exported status is recorded as a new entry in manifest.json, the one machine-readable registry sessions and tooling consult (registry-source-of-truth); and INSTALL.md's export-gate question in §3 is explicitly amended to carve out these two files, directly extending the export-loop mechanism (practice-export-loop)."}

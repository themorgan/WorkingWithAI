{"slugs": ["mistakes-become-rules", "cite-the-incident", "practice-export-loop", "scrub-gate", "doc-references-are-links"], "reasoning": "A recurring real cost (stray __pycache__ with nowhere for an ignore rule to live) is root-caused and encoded as a durable prevention — a template plus an install step — and the added PRACTICES.md text carries the originating incident inline with the rule. The lesson arrived abstracted from a dependent repo's check-in, so the upstream export loop and the public-safety scrub of that repo's identity are both in play, and the document edits reference the new template and INSTALL.md as relative markdown links per the repo's standing convention."}

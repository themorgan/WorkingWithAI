{"slugs": ["mistakes-become-rules", "cite-the-incident"], "reasoning": "A defect escaped the guard shipped in #36 and the commit itself states the general lesson, so the miss should be root-caused and the prevention encoded (mistakes-become-rules), and any rule written from it must carry the incident that motivated it inline, as the code comment already does (cite-the-incident). The auto-surfaced practice-export-loop does not apply: this is a bug fix to a tool in the upstream repo itself, not a generic practice improvement being folded back from a vendored copy at merge time."}

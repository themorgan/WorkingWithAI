{"slugs": ["mistakes-become-rules", "cite-the-incident"], "reasoning": "A guard shipped two steps earlier caused a false positive, so the fix is exactly a caught mistake whose root cause (a gate tested in isolation rather than across the whole loop) needs encoding as a lesson, and the inline comment and commit message do that by recording the originating incident alongside the rule it justifies. practice-export-loop does not fit: this is the upstream repo itself and the change is a bug fix in the check-in machinery, not an improvement to a generic practice that must be abstracted into a dependent repo's vendored tree."}

{"slugs": ["cite-the-incident", "mistakes-become-rules", "layered-practice-packs", "search-by-purpose", "doc-references-are-links", "docs-are-current-state", "no-version-suffix"], "reasoning": "The change encodes a new install-time rule prompted by a defect a dependent repo's check-in surfaced, so the incident must be recorded inline with the rule and root-caused into prevention; it also decides that the rule belongs in the generic upstream layer rather than repo-local, requires checking what the repo already covers (practice 8's existing .gitignore stanzas and deck/.gitignore) before adding, names two new files, and edits INSTALL.md and PRACTICES.md, where references must be relative links and the text must read as current state rather than a change log."}

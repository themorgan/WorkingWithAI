{"slugs": ["convention-to-audit", "cite-the-incident", "computed-numbers-in-scripts", "docs-track-models", "scripts-assert-properties", "doc-references-are-links", "docs-are-current-state"], "reasoning": "The change turns a stated-but-ungated rule into a script that exits non-zero (convention-to-audit) and writes that new enforcement rule into PRACTICES.md with the false-positive incident that shaped it (cite-the-incident), so the rule being enforced and the sentinel/doc_sync mechanism it extends must both be loaded verbatim (docs-track-models is practice 33 itself, computed-numbers-in-scripts is the generated-block gate it builds on), and the owned_figures() opt-in is the same per-script self-declaration shape as scripts-assert-properties. The PRACTICES.md prose edit brings the document practices into play: doc-references-are-links for how the new text names tools/doc_sync.py, and docs-are-current-state to keep the paragraph a statement of what is true now rather than a 'now enforced' change annotation."}

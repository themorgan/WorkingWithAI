{"slugs": ["session-bootstrap", "convention-to-audit", "cite-the-incident", "parallel-artifact-ledger", "volatile-rules-carry-dates", "no-version-suffix", "doc-references-are-links", "docs-are-current-state", "label-describes-content"], "reasoning": "The change extends the session-bootstrap practice to the other end of a turn and promotes an unenforced git-hygiene convention into a script that exits non-zero, so the new rule text in PRACTICES.md needs the incident it prevents, the adapter matrix needs a per-harness verdict for the new teardown check, and the claims about what Claude Code and managed environments provide need dates; a new file is named, and the three markdown files edited need relative links, current-state phrasing, and labels that match content (the recipe's 'three questions' becoming four, the new table column)."}

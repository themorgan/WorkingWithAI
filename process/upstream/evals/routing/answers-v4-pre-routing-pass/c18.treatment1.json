{"slugs": ["cite-the-incident", "layered-practice-packs", "doc-references-are-links"], "reasoning": "The change writes two new conventions into PRACTICES.md, so the rule-authoring practices apply: cite-the-incident (record the failure each new rule prevents) and layered-practice-packs (deciding these belong in the generic upstream layer rather than a repo-local one). It is also document editing that references repo files, so doc-references-are-links applies — the new text uses bare backticked `GLOSSARY.md` where a relative link is required. The other surfaced document practices (acronyms, generated numbers, current-state, labels, section order, reader vocabulary, export loop) have no purchase on this diff, and two-check-levels / merge-authorization-keyword are the practices being written here, not guidance for writing them."}

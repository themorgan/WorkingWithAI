{"slugs": ["practice-export-loop", "scrub-gate", "environment-gotchas", "cite-the-incident", "volatile-rules-carry-dates", "doc-references-are-links", "docs-are-current-state"], "reasoning": "This is an abstractive check-in of two generic practices plus a template fix from a private dependent repo into the public upstream, so the export loop and the scrub gate both govern it; the apt-retry finding is exactly an expensive environment discovery, whose incident must be recorded inline with the new rule and whose truth depends on external tooling, so it carries its 2026-08-06 date. The remaining two are the ordinary document-editing rules for the markdown being written (relative links, no change annotations). The other path-surfaced practices do not fit: nothing here is script-computed, no outward-facing entry document or reader-vocabulary pass is at stake, and section ordering is fixed by the practice numbering."}

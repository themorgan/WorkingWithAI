{"slugs": ["convention-to-audit", "cite-the-incident", "docs-track-models", "computed-numbers-in-scripts", "doc-references-are-links"], "reasoning": "The change is convention-to-audit in its pure form -- a prose rule (docs-track-models rule 2) that cost something is promoted to a non-zero exit, with the origin story kept in the script's docstring, which is cite-the-incident; the check is built inside the computed-numbers-in-scripts machinery (sentinels, the wired document/script registry, the drift gate) so that practice's contract constrains the implementation, and doc-references-are-links governs the PRACTICES.md prose being edited, which refers to the sync tool without linking it. Dropped scripts-assert-properties (this adds a gate in a tool, not property/anchor assertions on a script's own computed numbers -- only the opt-in-registry shape is analogous) and docs-are-current-state (the added text is a statement of what is now true, with no change-log annotation)."}

{
  "format_version": 1,
  "_comment": [
    "This file does NOT belong in a shared project. It is the user-level",
    "config, kept in your own environment -- by default at",
    "~/.config/precedent/config.json, or wherever PRECEDENT_USER_CONFIG",
    "points. It names your personal set, which nobody else can read.",
    "A shared project declares only universal and team sources, in its own",
    "tracked precedent.json; the tools refuse a personal set declared there."
  ],
  "individual": {
    "name": "an-example-personal-set",
    "path": "~/example-personal-set"
  }
}
